# Blocked IPs Configuration
# Bu dosya fail2ban tarafından otomatik olarak güncellenir

# Örnek bloklanmış IP'ler (fail2ban tarafından eklenecek)
# deny 192.168.1.100;
# deny 10.0.0.50;

# Bilinen kötü IP aralıkları (opsiyonel)
# deny 185.220.100.0/22;  # Tor exit nodes
# deny 198.98.51.0/24;    # Known botnet

# Whitelist (her zaman izin verilen IP'ler)
# allow 192.168.1.0/24;   # Local network
# allow 10.0.0.0/8;       # Private network

# Default: tüm IP'lere izin ver (fail2ban kuralları öncelikli)
allow all;