{
  "name": "zeytin-analiz-sistemi",
  "version": "1.0.0",
  "description": "AI destekli zeytin bahçesi analizi ve raporlama sistemi",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "test": "python -m pytest tests/ -v",
    "test:gpu": "python -m pytest tests/test_gpu.py -v",
    "test:validation": "python -m pytest tests/test_validation_edge_cases.py -v",
    "test:backup": "python -m pytest tests/test_backup.py -v",
    "backup": "bash scripts/backup_cron.sh",
    "deploy": "sudo bash scripts/deploy.sh",
    "setup:cuda": "sudo bash scripts/cuda_setup.sh",
    "health": "curl -f http://localhost:8000/health",
    "metrics": "curl -f http://localhost:8000/metrics"
  },
  "keywords": [
    "ai",
    "machine-learning",
    "agriculture",
    "olive-tree",
    "analysis",
    "fastapi",
    "yolo",
    "ndvi",
    "multispectral"
  ],
  "author": "Zeytin Analiz Sistemi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/zeytin-analiz-sistemi.git"
  },
  "engines": {
    "python": ">=3.10"
  }
}